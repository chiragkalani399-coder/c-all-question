// to find the cube of a number
#include <stdio.h>
int main()
{
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    printf("the cube of the number is %d", n * n * n);
    return 0;
}