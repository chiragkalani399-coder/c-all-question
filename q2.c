// Area of the square
#include <stdio.h>
int main()
{
    int s;
    printf("Enter the side of the square: ");
    scanf("%d", &s);
    printf("Area of the square is %d", s * s);
    return 0;
}